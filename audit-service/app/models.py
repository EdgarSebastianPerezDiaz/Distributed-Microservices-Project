#Contiene lo que se va  arecibir en un documento listo para guardar

import uuid

##def build_audit_document(event_data: dict) -> dict:
    ##return{
      ##  "eventId": str(uuid.uuid4()), #genera id automaticamente
        ##"entityType": event_data["entityType"],
        ##"entityId":event_data["entityId"],
        ##"operation":event_data["operation"],
        ##"description":event_data["description"],
        ##"timestamp": event_data["timestamp"],
        ##"performedBy": event_data["performedBy"],
        ##"userRole":event_data["UserRole"],
        ##"version":event_data["version"],
        ##"details":event_data.get("details",{}) ##devuelve el elemento a insertar
    ##}
